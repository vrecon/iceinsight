import type { CapacitorConfig } from '@capacitor/core';

const config: CapacitorConfig = {
  appId: 'nl.templify.iceinsights',
  appName: 'IceInsights',
  webDir: 'www',
  server: {
    androidScheme: 'https',
  },
};

export default config;
